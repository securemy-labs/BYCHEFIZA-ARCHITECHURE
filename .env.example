# ==============================================================================
# BYCHEFIZA Environment Configuration
# ==============================================================================
# 
# IMPORTANT SECURITY NOTES:
# - This is an EXAMPLE file. Copy to .env and update with your actual values.
# - NEVER commit .env files or secrets to source control.
# - Use a secrets manager (AWS Secrets Manager, HashiCorp Vault) in production.
# - Rotate secrets regularly and use strong, unique values for each environment.
# - Keep development, staging, and production secrets completely separate.
#
# ==============================================================================

# Application Configuration
NODE_ENV=development
APP_NAME=BYCHEFIZA
PORT=3000
BACKEND_PORT=3000

# Database Configuration
# PostgreSQL
DATABASE_URL=postgresql://admin:changeme@localhost:5432/bychefiza
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=bychefiza
POSTGRES_USER=admin
POSTGRES_PASSWORD=changeme

# MongoDB
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_DB=bychefiza
MONGO_USER=admin
MONGO_PASSWORD=changeme
# Alternative MongoDB connection string format:
# MONGODB_URI=mongodb://admin:changeme@localhost:27017/bychefiza

# Database Connection Pool Settings (adjust based on load)
DB_POOL_MIN=2
DB_POOL_MAX=10

# Redis Configuration
REDIS_URL=redis://localhost:6379
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=changeme
REDIS_DB=0

# RabbitMQ Configuration (Message Queue)
RABBITMQ_URL=amqp://guest:guest@localhost:5672
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

# JWT Configuration
# WARNING: Use a strong, randomly generated secret in production!
# Generate with: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRATION=24h
JWT_REFRESH_EXPIRATION=7d

# API Gateway Configuration
API_GATEWAY_PORT=3000
AUTH_SERVICE_URL=http://localhost:3001
USER_SERVICE_URL=http://localhost:3002
PRODUCT_SERVICE_URL=http://localhost:3003
ORDER_SERVICE_URL=http://localhost:3004
PAYMENT_SERVICE_URL=http://localhost:3005

# Service Ports
AUTH_SERVICE_PORT=3001
USER_SERVICE_PORT=3002
PRODUCT_SERVICE_PORT=3003
ORDER_SERVICE_PORT=3004
PAYMENT_SERVICE_PORT=3005

# AWS Configuration (Optional)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
S3_BUCKET_NAME=bychefiza-assets

# Payment Gateway (Stripe Example)
STRIPE_PUBLIC_KEY=pk_test_xxxxx
STRIPE_SECRET_KEY=sk_test_xxxxx

# Email Service (SendGrid Example)
SENDGRID_API_KEY=your-sendgrid-api-key
FROM_EMAIL=noreply@bychefiza.com

# Error Tracking and Monitoring
# Sentry DSN for error tracking (get from sentry.io)
SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
SENTRY_ENVIRONMENT=development
SENTRY_TRACES_SAMPLE_RATE=1.0

# Logging
LOG_LEVEL=info
# Options: error, warn, info, http, verbose, debug, silly

# Monitoring and Observability
PROMETHEUS_PORT=9090
GRAFANA_PORT=3005

# ==============================================================================
# Additional Notes:
# ==============================================================================
# 
# - Copy this file to .env: cp .env.example .env
# - Update .env with your actual values (never commit .env to git)
# - For production: use environment-specific configs and secret management
# - Consider using dotenv-vault or similar tools for team secret sharing
# - Validate required env vars on application startup
#
# ==============================================================================
